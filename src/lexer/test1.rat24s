[* this is sample code which finds the sum of two numbers in <10 lines of code *]
$
function findSum(a integer, b integer)
{
    return a + b;
}
$
integer result;
result = findSum(1, 2); 
print(result);
$